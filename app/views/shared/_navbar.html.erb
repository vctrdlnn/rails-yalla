<!-- PRELOADER -->
  <div class="page-loader">
    <div class="loader">Loading...</div>
  </div>
  <!-- /PRELOADER -->

  <!-- NAVIGATION -->
  <nav class="navbar navbar-custom navbar-fixed-top navbar-transparent" role="navigation">
    <div class="container-fluid">
      <div class="navbar-header">
        <div class="navbar-header"><%= link_to "YALA!", root_path, class: "navbar-brand navbar-link" %>
          <button data-toggle="collapse" data-target="#custom-collapse" class="navbar-toggle collapsed"><span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span></button>
        </div>
      </div>

      <% if user_signed_in? %>
        <!-- Right nav bar user sign in -->
        <div class="collapse navbar-collapse" id="custom-collapse">
          <ul class="nav navbar-nav navbar-right">
            <li class="dropdown" role="presentation"><%= link_to("My trips", my_trips_path)%></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name %></a>
              <ul class="dropdown-menu" role="menu">

                <li class="dropdown">
                  <%= link_to("My profile", edit_user_registration_path)%>
                </li>

                <li class="dropdown">
                  <%= link_to("Sign out", destroy_user_session_path, method: :delete) %>
                </li>
              </ul>
            </li>

          </ul>
        </div>
      <% else %>
        <!-- Right nav bar visitor -->
        <div class="collapse navbar-collapse" id="custom-collapse">
          <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
              <ul class="dropdown-menu" role="menu">
                <li class="dropdown">
                  <%= link_to("sign in", new_user_session_path, class: "navbar-link login") %>
                </li>
                <li class="dropdown">
                  <%= link_to("sign up", new_user_registration_path, class: "btn btn-g btn-round action-button", role: "button") %>
                </li>
              </ul>
            </li>
            <p class="navbar-text navbar-right actions ">
              <%= link_to "sign in", new_user_session_path, class: "navbar-link login" %>
              <%= link_to "sign up", new_user_registration_path, class: "btn btn-g btn-round action-button", role: "button" %>
            </p>
        </div>
      <% end %>
    </div>
  </nav>
